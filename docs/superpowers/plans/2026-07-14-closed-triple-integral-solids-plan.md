# 累次上下界三重积分闭合实体实施计划

1. 在 `plotSpec.test.ts` 增加单位圆柱与单位立方体失败用例，按 `meta.boundaryKind` 验证边界类型及侧壁坐标。
2. 在 `plotSpec.ts` 为三重积分抽取统一 surface trace 构造器、有效跨度检测与侧壁采样逻辑。
3. 保留内层上下表面，补齐中层上下侧壁及外层上下端面；跳过退化或完全非有限的表面。
4. 将三重积分材质改为不透明统一配色，并维持现有相机、坐标轴与交互。
5. 运行定向单元测试、完整测试与生产构建。
6. 通过 Playwright 在 `http://localhost:5173/#/` 输入单位圆柱，验证闭合视图、控制台健康及移动端布局，截图保存在仓库外。
