# 数学输入、条件区域和计算控制实施计划

1. 建立 MathLive 输入焦点上下文，把主公式、被积函数、变量、上下限、参数方程和向量场统一为数学输入组件，并让共用键盘跟随焦点。
2. 扩展二重/三重积分规格，增加累次边界与条件区域两种模式，实现约束列表、扫描范围、公式生成和持久化。
3. 增加约束解析器以及二维遮罩、三维隐式区域绘图，接入条件区域数值积分负载。
4. 重构 Python 请求生命周期，移除 20 秒硬终止，增加手动停止、耗时显示和数值优先重试。
5. 补充焦点路由、约束解析、绘图、请求取消和回归测试，完成桌面/手机浏览器验收与提交。
