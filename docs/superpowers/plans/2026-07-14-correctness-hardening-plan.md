# 积分计算台正确性加固实施计划

1. 建立完整公式解析结果与规范指纹，修复计算竞态并移除不安全的示例结果恢复。
2. 加入本地状态 schema 校验、容错写入与对应测试。
3. 区分符号未解、发散和定义域错误，修复条件区域数值掩码，增强 Pyodide 恢复。
4. 限制不等价的区域转换，拒绝面积/体积中的孤立等式，容忍局部无效绘图样本。
5. 延迟高成本预览，暂停空闲 Three.js 渲染，关闭生产公开 source map。
6. 运行单测、类型检查、生产构建与可用的浏览器核心流程验证。
